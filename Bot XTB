import MetaTrader5 as mt5
import pandas as pd
import time

# ─────────────────────────────────────────────────────────────
# 1. Conectar a MetaTrader5 usando los datos de tu cuenta Exness
# ─────────────────────────────────────────────────────────────
login = 197297510
server = "Exness-MT5Trial11"
password = "197297510"  # REEMPLAZA con tu contraseña real

if not mt5.initialize(login=login, server=server, password=password):
    print("❌ Error al inicializar MT5:", mt5.last_error())
    quit()
else:
    print("✅ Conexión a MetaTrader5 establecida")

# Seleccionar el símbolo (Bitcoin). Revisa que el nombre del símbolo sea correcto.
symbol = "BTCUSD"
if not mt5.symbol_select(symbol, True):
    print(f"❌ No se pudo seleccionar el símbolo {symbol}")
    mt5.shutdown()
    quit()

# ─────────────────────────────────────────────────────────────
# 2. Función para obtener datos (candles) en H1
# ─────────────────────────────────────────────────────────────
def get_candles(symbol, timeframe, count):
    """
    Obtiene los últimos 'count' candles para 'symbol' en el timeframe indicado.
    """
    rates = mt5.copy_rates_from_pos(symbol, timeframe, 0, count)
    if rates is None:
        print("❌ Error al obtener datos de", symbol)
        return None
    df = pd.DataFrame(rates)
    df['time'] = pd.to_datetime(df['time'], unit='s')
    return df

# ─────────────────────────────────────────────────────────────
# 3. Función para calcular la estrategia UT Bot Alerts (versión simplificada)
# ─────────────────────────────────────────────────────────────
def calculate_ut_bot(df, key_value=1, atr_period=10):
    """
    Calcula un trailing stop y genera una señal de posición (1 para compra, -1 para venta)
    basándose en una versión simplificada de la UT Bot Alerts.
    """
    if df is None or df.empty:
        return None
    
    df = df.copy()
    # Calcular una aproximación del ATR usando el rango verdadero (aquí se usa high-low) y su media móvil
    df['true_range'] = df['high'] - df['low']
    df['atr'] = df['true_range'].rolling(window=atr_period).mean()
    df['atr'].fillna(df['true_range'], inplace=True)
    df['n_loss'] = key_value * df['atr']
    
    # Usamos el precio de cierre como fuente
    df['src'] = df['close']
    
    # Inicializar columnas de trailing stop y señal
    df['xATRTrailingStop'] = 0.0
    df['pos'] = 0
    
    # Condición inicial: el trailing stop se iguala al primer cierre
    df.at[df.index[0], 'xATRTrailingStop'] = df['src'].iloc[0]
    df.at[df.index[0], 'pos'] = 0
    
    # Recorrer los datos a partir del segundo candle
    for i in range(1, len(df)):
        prev_stop = df.at[df.index[i-1], 'xATRTrailingStop']
        prev_src = df.at[df.index[i-1], 'src']
        current_src = df.at[df.index[i], 'src']
        current_n_loss = df.at[df.index[i], 'n_loss']
        
        # Replicar la lógica del UT Bot Alerts (adaptada)
        if current_src > prev_stop and prev_src > prev_stop:
            new_stop = max(prev_stop, current_src - current_n_loss)
        elif current_src < prev_stop and prev_src < prev_stop:
            new_stop = min(prev_stop, current_src + current_n_loss)
        else:
            if current_src > prev_stop:
                new_stop = current_src - current_n_loss
            else:
                new_stop = current_src + current_n_loss
        
        df.at[df.index[i], 'xATRTrailingStop'] = new_stop
        
        # Generar la señal:
        # Si el candle anterior estuvo por debajo del stop y el actual sube por encima → señal de compra (1)
        # Si el candle anterior estuvo por encima del stop y el actual cae por debajo → señal de venta (-1)
        if prev_src < prev_stop and current_src > new_stop:
            df.at[df.index[i], 'pos'] = 1
        elif prev_src > prev_stop and current_src < new_stop:
            df.at[df.index[i], 'pos'] = -1
        else:
            df.at[df.index[i], 'pos'] = df.at[df.index[i-1], 'pos']
    
    return df

# ─────────────────────────────────────────────────────────────
# 4. Función para enviar órdenes a través de MT5
# ─────────────────────────────────────────────────────────────
def place_order(symbol, order_type, volume):
    """
    Envía una orden de mercado (compra o venta) para el símbolo indicado.
    """
    # Obtener el precio actual
    if order_type == "BUY":
        price = mt5.symbol_info_tick(symbol).ask
        mt5_order_type = mt5.ORDER_TYPE_BUY
    elif order_type == "SELL":
        price = mt5.symbol_info_tick(symbol).bid
        mt5_order_type = mt5.ORDER_TYPE_SELL
    else:
        print("❌ Tipo de orden inválido")
        return None

    # Preparar la solicitud de orden
    request = {
        "action": mt5.TRADE_ACTION_DEAL,
        "symbol": symbol,
        "volume": volume,
        "type": mt5_order_type,
        "price": price,
        "deviation": 20,
        "magic": 234000,
        "comment": "Orden UT Bot Alerts",
        "type_time": mt5.ORDER_TIME_GTC,
        "type_filling": mt5.ORDER_FILLING_FOK,
    }

    # Enviar la orden
    result = mt5.order_send(request)
    return result

# ─────────────────────────────────────────────────────────────
# 5. Función principal del bot
# ─────────────────────────────────────────────────────────────
def run_bot():
    print("🚀 Ejecutando UT Bot para Exness (MT5) en Bitcoin (H1)")
    
    # Obtener los últimos 100 candles en H1
    df = get_candles(symbol, mt5.TIMEFRAME_H1, 100)
    if df is None:
        return
    
    # Calcular la estrategia UT Bot Alerts
    df = calculate_ut_bot(df, key_value=1, atr_period=10)
    if df is None:
        return
    
    # Mostrar el último dato calculado (puedes imprimir df.tail() para depurar)
    last_row = df.iloc[-1]
    signal = last_row['pos']
    print(f"Última señal calculada: {signal}")
    
    # Ejemplo: si la señal es 1 → compra; si es -1 → venta; si es 0 → sin acción.
    volume = 0.01  # Ajusta el volumen según tus necesidades y tamaño de cuenta
    if signal == 1:
        print("✅ Señal de COMPRA detectada")
        result = place_order(symbol, "BUY", volume)
        print("Resultado de la orden:", result)
    elif signal == -1:
        print("❌ Señal de VENTA detectada")
        result = place_order(symbol, "SELL", volume)
        print("Resultado de la orden:", result)
    else:
        print("ℹ️ Sin señal clara, no se ejecuta orden.")

# ─────────────────────────────────────────────────────────────
# 6. Ejecutar el bot y cerrar la conexión
# ─────────────────────────────────────────────────────────────
if __name__ == "__main__":
    run_bot()
    mt5.shutdown()
